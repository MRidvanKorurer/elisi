/** Storefront içerik sütunu — Container maxWidth yerine kullanılır */
export const CONTENT_MAX_WIDTH = 1480;

export const contentContainerSx = {
  maxWidth: CONTENT_MAX_WIDTH,
  width: '100%',
  mx: 'auto',
  px: { xs: 2, sm: 3, md: 4 }
};
